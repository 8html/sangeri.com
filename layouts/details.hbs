---
layout: default.hbs
---
<div class="content menupad {{pagetype}}">
  <div class="sliderkit ip-slider">
    <div class="sliderkit-nav">
      <div class="sliderkit-nav-clip">
        <h3>{{which imagestitle title}}</h3>
        <ul>
          {{#if images}}
            {{#eachIndex images}}
              <li><a href="#"><img src="{{item}}"></a></li>
            {{/eachIndex}}
          {{else}}
            {{#if list}}
              {{#eachIndex list}}
                {{#is item.data.slide true}}
                  <li><a href="#"><img src="{{item.data.image}}"></a></li>
                {{/is}}
              {{/eachIndex}}
            {{/if}}
          {{/if}}
        </ul>
      </div>
    </div>
    <div class="sliderkit-panels">
      <div class="sliderkit-btn sliderkit-go-btn sliderkit-go-prev"><a href="#" title="Previous"><span>Previous</span></a></div>
      <div class="sliderkit-btn sliderkit-go-btn sliderkit-go-next"><a href="#" title="Next"><span>Next</span></a></div>
      {{#if images}}
        {{#eachIndex images}}
          <div class="sliderkit-panel">
            <img src="{{item}}">
          </div>
        {{/eachIndex}}
      {{else}}
        {{#if list}}
          {{#eachIndex list}}
            {{#is item.data.slide true}}
              <div class="sliderkit-panel">
                <img src="{{item.data.image}}">
              </div>
            {{/is}}
          {{/eachIndex}}
        {{/if}}
      {{/if}}
    </div>
    <div class="sbhead"></div>
  </div>
  <div class="ip-content">
    <div class="sidebar">
      <div class="sb-head">
        <a class="left-margin" href="{{coltitlelink}}">
          {{coltitle}}
          <div class="sb-triangle">
            <div class="sb-triangle-bottom"></div>
          </div>
        </a>
      </div>
      <div class="sb-list left-margin">
        {{#if sidelist}}
          {{#eachProperty sidelist}}
            <a href="{{get value}}"{{#startsWith ../permalink value}} class="active"{{/startsWith}}>{{key}} <em>&#10095;</em></a>
          {{/eachProperty}}
          <div class="clearfix"></div>
        {{else}}
          {{#if list}}
            {{#eachIndexRemoveIndexDotHTML list}}
              <a href="{{item.filename}}"{{#startsWith ../permalink item.filename}} class="active"{{/startsWith}}>{{item.data.title}} <em class="triangle-right">&gt;</em></a>
            {{/eachIndexRemoveIndexDotHTML}}
            <div class="clearfix"></div>
          {{/if}}
        {{/if}}
      </div>
      <div class="contactbox">
        <a href="/contact/" class="contactimg left-margin">联系我们</a>
        {{>partial.contact.qq}}
      </div>
    </div>
    <div class="main">
      {{>body}}
    </div>
    <div class="clearfix"></div>
  </div>
</div>
